export enum TicketType {
    TASK = 1,
    BUG = 2,
    STORY = 3,
    EPIC = 4,
    SUBTASK = 5.
}

export enum TicketStatus {
    OPEN = 1,
    IN_PROGRESS = 2,
    CLOSED = 3,
}

export enum TicketPriority {
    HIGH = 1,
    MEDIUM = 2,
    LOW = 3,
}
