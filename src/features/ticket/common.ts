export enum TicketType {
    BUG = 1,
    FEATURE_REQUEST = 2,
    TASK = 3,
}

export enum TicketStatus {
    OPEN = 1,
    IN_PROGRESS = 2,
    CLOSED = 3,
}

export enum TicketPriority {
    LOW = 1,
    MEDIUM = 2,
    HIGH = 3,
}
